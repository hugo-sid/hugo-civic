---
title: "Components"
description: "Every USWDS component the theme exposes, and the shortcode or front matter that puts it on a page."
weight: 30
collection_variant: "condensed"
---

Components come in two shapes. Anything that sits inside body copy is a
shortcode you write in the markdown. Anything that wraps the page — navigation,
the header, the footer — is chosen in `hugo.toml` or in front matter, because a
shortcode can only ever render inside `main`.
